<% if user_signed_in? && favorited = current_user.favorited?(space) %>
  <%= link_to 'Unfavorite', favorite_path(favorited), method: :delete %>
<% else %>
  <%= simple_form_for([space, favorite]) do |f| %>
    <%= f.submit 'Favorite' %>
  <% end %>
<% end %>
